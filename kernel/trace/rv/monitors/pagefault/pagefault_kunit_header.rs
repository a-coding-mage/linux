/* SPDX-License-Identifier: GPL-2.0-only */
/*
 * Automatically generated by rvgen kunit.
 * May need manual intervention for function prototypes that couldn't be
 * found (e.g. are in another file) or variables to be exported.
 */

// Header guard: __PAGEFAULT_KUNIT_H

// Equivalent to: #if IS_ENABLED(CONFIG_RV_MONITORS_KUNIT_TEST)

use core::ffi::c_void;

#[repr(C)]
pub struct RvPagefaultOps {
    pub mon: crate::rv_kunit_mon,
    pub handle_page_fault: Option<unsafe extern "C" fn(
        data: *mut c_void,
        address: usize,
        regs: *mut crate::pt_regs,
        error_code: usize,
    )>,
    pub handle_task_newtask: Option<unsafe extern "C" fn(
        data: *mut c_void,
        task: *mut crate::task_struct,
        flags: u64,
    )>,
}

unsafe extern "C" {
    pub static rv_pagefault_ops: RvPagefaultOps;
}

// End of: #if IS_ENABLED(CONFIG_RV_MONITORS_KUNIT_TEST)

// SOURCE-COMMIT: d482bb509b7d065808de40ce78b5bca39f40b783
