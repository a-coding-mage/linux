/* SPDX-License-Identifier: MIT */

// Dependencies supplied by Xen interface headers:
// RING_IDX, grant_ref_t, and the ring definitions generated by
// DEFINE_XEN_FLEX_RING/DEFINE_RING_TYPES.

pub const XENBUS_FUNCTIONS_CALLS: &[u8] = b"1\0";

#[repr(C)]
pub struct pvcalls_data_intf {
    pub in_cons: RING_IDX,
    pub in_prod: RING_IDX,
    pub in_error: RING_IDX,
    pub pad1: [u8; 52],
    pub out_cons: RING_IDX,
    pub out_prod: RING_IDX,
    pub out_error: RING_IDX,
    pub pad2: [u8; 52],
    pub ring_order: RING_IDX,
    pub r#ref: [grant_ref_t; 0],
}

// DEFINE_XEN_FLEX_RING(pvcalls);

pub const PVCALLS_SOCKET: u32 = 0;
pub const PVCALLS_CONNECT: u32 = 1;
pub const PVCALLS_RELEASE: u32 = 2;
pub const PVCALLS_BIND: u32 = 3;
pub const PVCALLS_LISTEN: u32 = 4;
pub const PVCALLS_ACCEPT: u32 = 5;
pub const PVCALLS_POLL: u32 = 6;

#[repr(C)]
#[derive(Copy, Clone)]
pub struct xen_pvcalls_socket {
    pub id: u64,
    pub domain: u32,
    pub r#type: u32,
    pub protocol: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct xen_pvcalls_connect {
    pub id: u64,
    pub addr: [u8; 28],
    pub len: u32,
    pub flags: u32,
    pub r#ref: grant_ref_t,
    pub evtchn: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct xen_pvcalls_release {
    pub id: u64,
    pub reuse: u8,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct xen_pvcalls_bind {
    pub id: u64,
    pub addr: [u8; 28],
    pub len: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct xen_pvcalls_listen {
    pub id: u64,
    pub backlog: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct xen_pvcalls_accept {
    pub id: u64,
    pub id_new: u64,
    pub r#ref: grant_ref_t,
    pub evtchn: u32,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct xen_pvcalls_poll {
    pub id: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct xen_pvcalls_dummy {
    pub dummy: [u8; 56],
}

#[repr(C)]
pub union xen_pvcalls_request_u {
    pub socket: xen_pvcalls_socket,
    pub connect: xen_pvcalls_connect,
    pub release: xen_pvcalls_release,
    pub bind: xen_pvcalls_bind,
    pub listen: xen_pvcalls_listen,
    pub accept: xen_pvcalls_accept,
    pub poll: xen_pvcalls_poll,
    pub dummy: xen_pvcalls_dummy,
}

#[repr(C)]
pub struct xen_pvcalls_request {
    pub req_id: u32,
    pub cmd: u32,
    pub u: xen_pvcalls_request_u,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct _xen_pvcalls_socket {
    pub id: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct _xen_pvcalls_connect {
    pub id: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct _xen_pvcalls_release {
    pub id: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct _xen_pvcalls_bind {
    pub id: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct _xen_pvcalls_listen {
    pub id: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct _xen_pvcalls_accept {
    pub id: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct _xen_pvcalls_poll {
    pub id: u64,
}

#[repr(C)]
#[derive(Copy, Clone)]
pub struct _xen_pvcalls_dummy {
    pub dummy: [u8; 8],
}

#[repr(C)]
#[derive(Copy, Clone)]
pub union xen_pvcalls_response_u {
    pub socket: _xen_pvcalls_socket,
    pub connect: _xen_pvcalls_connect,
    pub release: _xen_pvcalls_release,
    pub bind: _xen_pvcalls_bind,
    pub listen: _xen_pvcalls_listen,
    pub accept: _xen_pvcalls_accept,
    pub poll: _xen_pvcalls_poll,
    pub dummy: _xen_pvcalls_dummy,
}

#[repr(C)]
pub struct xen_pvcalls_response {
    pub req_id: u32,
    pub cmd: u32,
    pub ret: i32,
    pub pad: u32,
    pub u: xen_pvcalls_response_u,
}

// DEFINE_RING_TYPES(xen_pvcalls, struct xen_pvcalls_request,
//                   struct xen_pvcalls_response);

// SOURCE-COMMIT: d482bb509b7d065808de40ce78b5bca39f40b783
