/* SPDX-License-Identifier: MIT */
/* Copyright (c) 2012-2020 NVIDIA Corporation */

/* Tegra DRM legacy UAPI. Only enabled with STAGING */

pub const DRM_TEGRA_GEM_CREATE_TILED: u32 = 1 << 0;
pub const DRM_TEGRA_GEM_CREATE_BOTTOM_UP: u32 = 1 << 1;

#[repr(C)]
pub struct drm_tegra_gem_create { pub size: u64, pub flags: u32, pub handle: u32 }
#[repr(C)]
pub struct drm_tegra_gem_mmap { pub handle: u32, pub pad: u32, pub offset: u64 }
#[repr(C)]
pub struct drm_tegra_syncpt_read { pub id: u32, pub value: u32 }
#[repr(C)]
pub struct drm_tegra_syncpt_incr { pub id: u32, pub pad: u32 }
#[repr(C)]
pub struct drm_tegra_syncpt_wait { pub id: u32, pub thresh: u32, pub timeout: u32, pub value: u32 }
pub const DRM_TEGRA_NO_TIMEOUT: u32 = 0xffffffff;
#[repr(C)]
pub struct drm_tegra_open_channel { pub client: u32, pub pad: u32, pub context: u64 }
#[repr(C)]
pub struct drm_tegra_close_channel { pub context: u64 }
#[repr(C)]
pub struct drm_tegra_get_syncpt { pub context: u64, pub index: u32, pub id: u32 }
#[repr(C)]
pub struct drm_tegra_get_syncpt_base { pub context: u64, pub syncpt: u32, pub id: u32 }
#[repr(C)]
pub struct drm_tegra_syncpt { pub id: u32, pub incrs: u32 }
#[repr(C)]
pub struct drm_tegra_cmdbuf { pub handle: u32, pub offset: u32, pub words: u32, pub pad: u32 }

#[repr(C)]
pub struct drm_tegra_reloc_cmdbuf { pub handle: u32, pub offset: u32 }
#[repr(C)]
pub struct drm_tegra_reloc_target { pub handle: u32, pub offset: u32 }
#[repr(C)]
pub struct drm_tegra_reloc {
    pub cmdbuf: drm_tegra_reloc_cmdbuf,
    pub target: drm_tegra_reloc_target,
    pub shift: u32,
    pub pad: u32,
}
#[repr(C)]
pub struct drm_tegra_waitchk { pub handle: u32, pub offset: u32, pub syncpt: u32, pub thresh: u32 }
#[repr(C)]
pub struct drm_tegra_submit {
    pub context: u64, pub num_syncpts: u32, pub num_cmdbufs: u32,
    pub num_relocs: u32, pub num_waitchks: u32, pub waitchk_mask: u32,
    pub timeout: u32, pub syncpts: u64, pub cmdbufs: u64, pub relocs: u64,
    pub waitchks: u64, pub fence: u32, pub reserved: [u32; 5],
}

pub const DRM_TEGRA_GEM_TILING_MODE_PITCH: u32 = 0;
pub const DRM_TEGRA_GEM_TILING_MODE_TILED: u32 = 1;
pub const DRM_TEGRA_GEM_TILING_MODE_BLOCK: u32 = 2;
#[repr(C)]
pub struct drm_tegra_gem_set_tiling { pub handle: u32, pub mode: u32, pub value: u32, pub pad: u32 }
#[repr(C)]
pub struct drm_tegra_gem_get_tiling { pub handle: u32, pub mode: u32, pub value: u32, pub pad: u32 }
pub const DRM_TEGRA_GEM_BOTTOM_UP: u32 = 1 << 0;
pub const DRM_TEGRA_GEM_FLAGS: u32 = DRM_TEGRA_GEM_BOTTOM_UP;
#[repr(C)]
pub struct drm_tegra_gem_set_flags { pub handle: u32, pub flags: u32 }
#[repr(C)]
pub struct drm_tegra_gem_get_flags { pub handle: u32, pub flags: u32 }

pub const DRM_TEGRA_GEM_CREATE: u32 = 0x00;
pub const DRM_TEGRA_GEM_MMAP: u32 = 0x01;
pub const DRM_TEGRA_SYNCPT_READ: u32 = 0x02;
pub const DRM_TEGRA_SYNCPT_INCR: u32 = 0x03;
pub const DRM_TEGRA_SYNCPT_WAIT: u32 = 0x04;
pub const DRM_TEGRA_OPEN_CHANNEL: u32 = 0x05;
pub const DRM_TEGRA_CLOSE_CHANNEL: u32 = 0x06;
pub const DRM_TEGRA_GET_SYNCPT: u32 = 0x07;
pub const DRM_TEGRA_SUBMIT: u32 = 0x08;
pub const DRM_TEGRA_GET_SYNCPT_BASE: u32 = 0x09;
pub const DRM_TEGRA_GEM_SET_TILING: u32 = 0x0a;
pub const DRM_TEGRA_GEM_GET_TILING: u32 = 0x0b;
pub const DRM_TEGRA_GEM_SET_FLAGS: u32 = 0x0c;
pub const DRM_TEGRA_GEM_GET_FLAGS: u32 = 0x0d;

/* DRM_IOCTL_* values are generated by the external DRM_IOWR macro. */
pub const DRM_IOCTL_TEGRA_GEM_CREATE: _ = DRM_IOWR!(DRM_COMMAND_BASE + DRM_TEGRA_GEM_CREATE, drm_tegra_gem_create);
pub const DRM_IOCTL_TEGRA_GEM_MMAP: _ = DRM_IOWR!(DRM_COMMAND_BASE + DRM_TEGRA_GEM_MMAP, drm_tegra_gem_mmap);
pub const DRM_IOCTL_TEGRA_SYNCPT_READ: _ = DRM_IOWR!(DRM_COMMAND_BASE + DRM_TEGRA_SYNCPT_READ, drm_tegra_syncpt_read);
pub const DRM_IOCTL_TEGRA_SYNCPT_INCR: _ = DRM_IOWR!(DRM_COMMAND_BASE + DRM_TEGRA_SYNCPT_INCR, drm_tegra_syncpt_incr);
pub const DRM_IOCTL_TEGRA_SYNCPT_WAIT: _ = DRM_IOWR!(DRM_COMMAND_BASE + DRM_TEGRA_SYNCPT_WAIT, drm_tegra_syncpt_wait);
pub const DRM_IOCTL_TEGRA_OPEN_CHANNEL: _ = DRM_IOWR!(DRM_COMMAND_BASE + DRM_TEGRA_OPEN_CHANNEL, drm_tegra_open_channel);
pub const DRM_IOCTL_TEGRA_CLOSE_CHANNEL: _ = DRM_IOWR!(DRM_COMMAND_BASE + DRM_TEGRA_CLOSE_CHANNEL, drm_tegra_close_channel);
pub const DRM_IOCTL_TEGRA_GET_SYNCPT: _ = DRM_IOWR!(DRM_COMMAND_BASE + DRM_TEGRA_GET_SYNCPT, drm_tegra_get_syncpt);
pub const DRM_IOCTL_TEGRA_SUBMIT: _ = DRM_IOWR!(DRM_COMMAND_BASE + DRM_TEGRA_SUBMIT, drm_tegra_submit);
pub const DRM_IOCTL_TEGRA_GET_SYNCPT_BASE: _ = DRM_IOWR!(DRM_COMMAND_BASE + DRM_TEGRA_GET_SYNCPT_BASE, drm_tegra_get_syncpt_base);
pub const DRM_IOCTL_TEGRA_GEM_SET_TILING: _ = DRM_IOWR!(DRM_COMMAND_BASE + DRM_TEGRA_GEM_SET_TILING, drm_tegra_gem_set_tiling);
pub const DRM_IOCTL_TEGRA_GEM_GET_TILING: _ = DRM_IOWR!(DRM_COMMAND_BASE + DRM_TEGRA_GEM_GET_TILING, drm_tegra_gem_get_tiling);
pub const DRM_IOCTL_TEGRA_GEM_SET_FLAGS: _ = DRM_IOWR!(DRM_COMMAND_BASE + DRM_TEGRA_GEM_SET_FLAGS, drm_tegra_gem_set_flags);
pub const DRM_IOCTL_TEGRA_GEM_GET_FLAGS: _ = DRM_IOWR!(DRM_COMMAND_BASE + DRM_TEGRA_GEM_GET_FLAGS, drm_tegra_gem_get_flags);
/* New Tegra DRM UAPI */
pub const DRM_TEGRA_CHANNEL_CAP_CACHE_COHERENT: u32 = 1 << 0;
#[repr(C)]
pub struct drm_tegra_channel_open { pub host1x_class: u32, pub flags: u32, pub context: u32, pub version: u32, pub capabilities: u32, pub padding: u32 }
#[repr(C)]
pub struct drm_tegra_channel_close { pub context: u32, pub padding: u32 }
pub const DRM_TEGRA_CHANNEL_MAP_READ: u32 = 1 << 0;
pub const DRM_TEGRA_CHANNEL_MAP_WRITE: u32 = 1 << 1;
pub const DRM_TEGRA_CHANNEL_MAP_READ_WRITE: u32 = DRM_TEGRA_CHANNEL_MAP_READ | DRM_TEGRA_CHANNEL_MAP_WRITE;
#[repr(C)]
pub struct drm_tegra_channel_map { pub context: u32, pub handle: u32, pub flags: u32, pub mapping: u32 }
#[repr(C)]
pub struct drm_tegra_channel_unmap { pub context: u32, pub mapping: u32 }
pub const DRM_TEGRA_SUBMIT_RELOC_SECTOR_LAYOUT: u32 = 1 << 0;
#[repr(C)]
pub struct drm_tegra_submit_buf { pub mapping: u32, pub flags: u32, pub reloc: drm_tegra_submit_buf_reloc }
#[repr(C)]
pub struct drm_tegra_submit_buf_reloc { pub target_offset: u64, pub gather_offset_words: u32, pub shift: u32 }
pub const DRM_TEGRA_SUBMIT_CMD_GATHER_UPTR: u32 = 0;
pub const DRM_TEGRA_SUBMIT_CMD_WAIT_SYNCPT: u32 = 1;
pub const DRM_TEGRA_SUBMIT_CMD_WAIT_SYNCPT_RELATIVE: u32 = 2;
#[repr(C)]
pub struct drm_tegra_submit_cmd_gather_uptr { pub words: u32, pub reserved: [u32; 3] }
#[repr(C)]
pub struct drm_tegra_submit_cmd_wait_syncpt { pub id: u32, pub value: u32, pub reserved: [u32; 2] }
#[repr(C)]
pub union drm_tegra_submit_cmd_data { pub gather_uptr: drm_tegra_submit_cmd_gather_uptr, pub wait_syncpt: drm_tegra_submit_cmd_wait_syncpt, pub reserved: [u32; 4] }
#[repr(C)]
pub struct drm_tegra_submit_cmd { pub type_: u32, pub flags: u32, pub data: drm_tegra_submit_cmd_data }
#[repr(C)]
pub struct drm_tegra_submit_syncpt { pub id: u32, pub flags: u32, pub increments: u32, pub value: u32 }
#[repr(C)]
pub struct drm_tegra_channel_submit {
    pub context: u32, pub num_bufs: u32, pub num_cmds: u32, pub gather_data_words: u32,
    pub bufs_ptr: u64, pub cmds_ptr: u64, pub gather_data_ptr: u64,
    pub syncobj_in: u32, pub syncobj_out: u32, pub syncpt: drm_tegra_submit_syncpt,
}
#[repr(C)]
pub struct drm_tegra_syncpoint_allocate { pub id: u32, pub padding: u32 }
#[repr(C)]
pub struct drm_tegra_syncpoint_free { pub id: u32, pub padding: u32 }
#[repr(C)]
pub struct drm_tegra_syncpoint_wait { pub timeout_ns: i64, pub id: u32, pub threshold: u32, pub value: u32, pub padding: u32 }
pub const DRM_IOCTL_TEGRA_CHANNEL_OPEN: _ = DRM_IOWR!(DRM_COMMAND_BASE + 0x10, drm_tegra_channel_open);
pub const DRM_IOCTL_TEGRA_CHANNEL_CLOSE: _ = DRM_IOWR!(DRM_COMMAND_BASE + 0x11, drm_tegra_channel_close);
pub const DRM_IOCTL_TEGRA_CHANNEL_MAP: _ = DRM_IOWR!(DRM_COMMAND_BASE + 0x12, drm_tegra_channel_map);
pub const DRM_IOCTL_TEGRA_CHANNEL_UNMAP: _ = DRM_IOWR!(DRM_COMMAND_BASE + 0x13, drm_tegra_channel_unmap);
pub const DRM_IOCTL_TEGRA_CHANNEL_SUBMIT: _ = DRM_IOWR!(DRM_COMMAND_BASE + 0x14, drm_tegra_channel_submit);
pub const DRM_IOCTL_TEGRA_SYNCPOINT_ALLOCATE: _ = DRM_IOWR!(DRM_COMMAND_BASE + 0x20, drm_tegra_syncpoint_allocate);
pub const DRM_IOCTL_TEGRA_SYNCPOINT_FREE: _ = DRM_IOWR!(DRM_COMMAND_BASE + 0x21, drm_tegra_syncpoint_free);
pub const DRM_IOCTL_TEGRA_SYNCPOINT_WAIT: _ = DRM_IOWR!(DRM_COMMAND_BASE + 0x22, drm_tegra_syncpoint_wait);

// SOURCE-COMMIT: d482bb509b7d065808de40ce78b5bca39f40b783
