/* Private i386 syscall transport for an ordinary ABI runtime attempt. */
typedef __SIZE_TYPE__ size_t;
extern int main(int, char **);
__attribute__((noreturn)) static void finish(int result)
{
    __asm__ volatile("int $0x80" : : "a"(1), "b"(result) : "memory");
    __builtin_unreachable();
}
__attribute__((noreturn)) void abort(void) { finish(97); }
int printf(const char *format, ...) { (void)format; return 0; }
void *memcpy(void *dst, const void *src, size_t n)
{
    unsigned char *d = dst;
    const unsigned char *s = src;
    for (size_t i = 0; i < n; ++i) d[i] = s[i];
    return dst;
}
void *memset(void *dst, int value, size_t n)
{
    unsigned char *d = dst;
    for (size_t i = 0; i < n; ++i) d[i] = value;
    return dst;
}
__attribute__((noreturn)) void suite(void)
{
    char *args[] = {"list_sort", (void *)0};
    finish(main(1, args));
}
__asm__(".text\n.globl _start\n_start:\n andl $-16,%esp\n call suite\n ud2\n");
