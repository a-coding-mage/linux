/*
 * Rust translation of asm/uasm.h.
 * C preprocessor configuration symbols are preserved in comments below.
 */

#[repr(C)]
pub struct uasm_label {
    pub addr: *mut u32,
    pub lab: i32,
}

#[repr(C)]
pub struct uasm_reloc {
    pub addr: *mut u32,
    pub type_: u32,
    pub lab: i32,
}

macro_rules! uasm_decl {
    ($name:ident, ()) => {
        pub fn $name(buf: *mut *mut u32);
    };
    ($name:ident, ($($arg:ident : $ty:ty),*)) => {
        pub fn $name(buf: *mut *mut u32, $($arg: $ty),*);
    };
}

// Instruction declarations generated by the C Ip_* declaration macros.
extern "C" {
    uasm_decl!(_addiu, (a: u32, b: u32, c: i32));
    uasm_decl!(_addu, (a: u32, b: u32, c: u32));
    uasm_decl!(_and, (a: u32, b: u32, c: u32));
    uasm_decl!(_andi, (a: u32, b: u32, c: u32));
    uasm_decl!(_bbit0, (a: u32, b: u32, c: i32));
    uasm_decl!(_bbit1, (a: u32, b: u32, c: i32));
    uasm_decl!(_beq, (a: u32, b: u32, c: i32));
    uasm_decl!(_beql, (a: u32, b: u32, c: i32));
    uasm_decl!(_bgez, (a: u32, b: i32));
    uasm_decl!(_bgezl, (a: u32, b: i32));
    uasm_decl!(_bgtz, (a: u32, b: i32));
    uasm_decl!(_blez, (a: u32, b: i32));
    uasm_decl!(_bltz, (a: u32, b: i32));
    uasm_decl!(_bltzl, (a: u32, b: i32));
    uasm_decl!(_bne, (a: u32, b: u32, c: i32));
    uasm_decl!(_break, (a: u32));
    uasm_decl!(_cache, (a: u32, b: i32, c: u32));
    uasm_decl!(_cfc1, (a: u32, b: u32));
    uasm_decl!(_cfcmsa, (a: u32, b: u32));
    uasm_decl!(_ctc1, (a: u32, b: u32));
    uasm_decl!(_ctcmsa, (a: u32, b: u32));
    uasm_decl!(_daddiu, (a: u32, b: u32, c: i32));
    uasm_decl!(_daddu, (a: u32, b: u32, c: u32));
    uasm_decl!(_ddivu, (a: u32, b: u32));
    uasm_decl!(_ddivu_r6, (a: u32, b: u32, c: u32));
    uasm_decl!(_di, (a: u32));
    uasm_decl!(_dins, (a: u32, b: u32, c: u32, d: u32));
    uasm_decl!(_dinsm, (a: u32, b: u32, c: u32, d: u32));
    uasm_decl!(_dinsu, (a: u32, b: u32, c: u32, d: u32));
    uasm_decl!(_divu, (a: u32, b: u32));
    uasm_decl!(_divu_r6, (a: u32, b: u32, c: u32));
    uasm_decl!(_dmfc0, (a: u32, b: u32, c: u32));
    uasm_decl!(_dmodu, (a: u32, b: u32, c: u32));
    uasm_decl!(_dmtc0, (a: u32, b: u32, c: u32));
    uasm_decl!(_dmultu, (a: u32, b: u32));
    uasm_decl!(_dmulu, (a: u32, b: u32, c: u32));
    uasm_decl!(_drotr, (a: u32, b: u32, c: u32));
    uasm_decl!(_drotr32, (a: u32, b: u32, c: u32));
    uasm_decl!(_dsbh, (a: u32, b: u32));
    uasm_decl!(_dshd, (a: u32, b: u32));
    uasm_decl!(_dsll, (a: u32, b: u32, c: u32));
    uasm_decl!(_dsll32, (a: u32, b: u32, c: u32));
    uasm_decl!(_dsllv, (a: u32, b: u32, c: u32));
    uasm_decl!(_dsra, (a: u32, b: u32, c: u32));
    uasm_decl!(_dsra32, (a: u32, b: u32, c: u32));
    uasm_decl!(_dsrav, (a: u32, b: u32, c: u32));
    uasm_decl!(_dsrl, (a: u32, b: u32, c: u32));
    uasm_decl!(_dsrl32, (a: u32, b: u32, c: u32));
    uasm_decl!(_dsrlv, (a: u32, b: u32, c: u32));
    uasm_decl!(_dsubu, (a: u32, b: u32, c: u32));
    uasm_decl!(_eret, ());
    uasm_decl!(_ext, (a: u32, b: u32, c: u32, d: u32));
    uasm_decl!(_ins, (a: u32, b: u32, c: u32, d: u32));
    uasm_decl!(_j, (a: u32));
    uasm_decl!(_jal, (a: u32));
    uasm_decl!(_jalr, (a: u32, b: u32));
    uasm_decl!(_jr, (a: u32));
    uasm_decl!(_lb, (a: u32, b: i32, c: u32));
    uasm_decl!(_lbu, (a: u32, b: i32, c: u32));
    uasm_decl!(_ld, (a: u32, b: i32, c: u32));
    uasm_decl!(_ldx, (a: u32, b: u32, c: u32));
    uasm_decl!(_lh, (a: u32, b: i32, c: u32));
    uasm_decl!(_lhu, (a: u32, b: i32, c: u32));
    uasm_decl!(_ll, (a: u32, b: i32, c: u32));
    uasm_decl!(_lld, (a: u32, b: i32, c: u32));
    uasm_decl!(_lui, (a: u32, b: i32));
    uasm_decl!(_lw, (a: u32, b: i32, c: u32));
    uasm_decl!(_lwu, (a: u32, b: i32, c: u32));
    uasm_decl!(_lwx, (a: u32, b: u32, c: u32));
    uasm_decl!(_mfc0, (a: u32, b: u32, c: u32));
    uasm_decl!(_mfhc0, (a: u32, b: u32, c: u32));
    uasm_decl!(_mfhi, (a: u32));
    uasm_decl!(_mflo, (a: u32));
    uasm_decl!(_modu, (a: u32, b: u32, c: u32));
    uasm_decl!(_movn, (a: u32, b: u32, c: u32));
    uasm_decl!(_movz, (a: u32, b: u32, c: u32));
    uasm_decl!(_mtc0, (a: u32, b: u32, c: u32));
    uasm_decl!(_mthc0, (a: u32, b: u32, c: u32));
    uasm_decl!(_mthi, (a: u32));
    uasm_decl!(_mtlo, (a: u32));
    uasm_decl!(_mul, (a: u32, b: u32, c: u32));
    uasm_decl!(_multu, (a: u32, b: u32));
    uasm_decl!(_mulu, (a: u32, b: u32, c: u32));
    uasm_decl!(_muhu, (a: u32, b: u32, c: u32));
    uasm_decl!(_nor, (a: u32, b: u32, c: u32));
    uasm_decl!(_or, (a: u32, b: u32, c: u32));
    uasm_decl!(_ori, (a: u32, b: u32, c: u32));
    uasm_decl!(_pref, (a: u32, b: i32, c: u32));
    uasm_decl!(_rfe, ());
    uasm_decl!(_rotr, (a: u32, b: u32, c: u32));
    uasm_decl!(_sb, (a: u32, b: i32, c: u32));
    uasm_decl!(_sc, (a: u32, b: i32, c: u32));
    uasm_decl!(_scd, (a: u32, b: i32, c: u32));
    uasm_decl!(_sd, (a: u32, b: i32, c: u32));
    uasm_decl!(_seleqz, (a: u32, b: u32, c: u32));
    uasm_decl!(_selnez, (a: u32, b: u32, c: u32));
    uasm_decl!(_sh, (a: u32, b: i32, c: u32));
    uasm_decl!(_sll, (a: u32, b: u32, c: u32));
    uasm_decl!(_sllv, (a: u32, b: u32, c: u32));
    uasm_decl!(_slt, (a: i32, b: i32, c: i32));
    uasm_decl!(_slti, (a: u32, b: u32, c: i32));
    uasm_decl!(_sltiu, (a: u32, b: u32, c: i32));
    uasm_decl!(_sltu, (a: u32, b: u32, c: u32));
    uasm_decl!(_sra, (a: u32, b: u32, c: u32));
    uasm_decl!(_srav, (a: u32, b: u32, c: u32));
    uasm_decl!(_srl, (a: u32, b: u32, c: u32));
    uasm_decl!(_srlv, (a: u32, b: u32, c: u32));
    uasm_decl!(_subu, (a: u32, b: u32, c: u32));
    uasm_decl!(_sw, (a: u32, b: i32, c: u32));
    uasm_decl!(_sync, (a: u32));
    uasm_decl!(_syscall, (a: u32));
    uasm_decl!(_tlbp, ());
    uasm_decl!(_tlbr, ());
    uasm_decl!(_tlbwi, ());
    uasm_decl!(_tlbwr, ());
    uasm_decl!(_wait, (a: u32));
    uasm_decl!(_wsbh, (a: u32, b: u32));
    uasm_decl!(_xor, (a: u32, b: u32, c: u32));
    uasm_decl!(_xori, (a: u32, b: u32, c: u32));
    uasm_decl!(_yield, (a: u32, b: u32));
    uasm_decl!(_ldpte, (a: u32, b: u32));
    uasm_decl!(_lddir, (a: u32, b: u32, c: u32));
}

extern "C" {
    pub fn uasm_build_label(lab: *mut *mut uasm_label, addr: *mut u32, lid: i32);
    pub fn uasm_in_compat_space_p(addr: isize) -> i32;
    pub fn uasm_rel_hi(val: isize) -> i32;
    pub fn uasm_rel_lo(val: isize) -> i32;
    pub fn UASM_i_LA_mostly(buf: *mut *mut u32, rs: u32, addr: isize);
    pub fn UASM_i_LA(buf: *mut *mut u32, rs: u32, addr: isize);

    pub fn uasm_r_mips_pc16(rel: *mut *mut uasm_reloc, addr: *mut u32, lid: i32);
    pub fn uasm_resolve_relocs(rel: *mut uasm_reloc, lab: *mut uasm_label);
    pub fn uasm_move_relocs(rel: *mut uasm_reloc, first: *mut u32, end: *mut u32, off: isize);
    pub fn uasm_move_labels(lab: *mut uasm_label, first: *mut u32, end: *mut u32, off: isize);
    pub fn uasm_copy_handler(rel: *mut uasm_reloc, lab: *mut uasm_label, first: *mut u32, end: *mut u32, target: *mut u32);
    pub fn uasm_insn_has_bdelay(rel: *mut uasm_reloc, addr: *mut u32) -> i32;

    pub fn uasm_il_b(p: *mut *mut u32, r: *mut *mut uasm_reloc, lid: i32);
    pub fn uasm_il_bbit0(p: *mut *mut u32, r: *mut *mut uasm_reloc, reg: u32, bit: u32, lid: i32);
    pub fn uasm_il_bbit1(p: *mut *mut u32, r: *mut *mut uasm_reloc, reg: u32, bit: u32, lid: i32);
    pub fn uasm_il_beq(p: *mut *mut u32, r: *mut *mut uasm_reloc, r1: u32, r2: u32, lid: i32);
    pub fn uasm_il_beqz(p: *mut *mut u32, r: *mut *mut uasm_reloc, reg: u32, lid: i32);
    pub fn uasm_il_beqzl(p: *mut *mut u32, r: *mut *mut uasm_reloc, reg: u32, lid: i32);
    pub fn uasm_il_bgezl(p: *mut *mut u32, r: *mut *mut uasm_reloc, reg: u32, lid: i32);
    pub fn uasm_il_bgez(p: *mut *mut u32, r: *mut *mut uasm_reloc, reg: u32, lid: i32);
    pub fn uasm_il_bltz(p: *mut *mut u32, r: *mut *mut uasm_reloc, reg: u32, lid: i32);
    pub fn uasm_il_bne(p: *mut *mut u32, r: *mut *mut uasm_reloc, reg1: u32, reg2: u32, lid: i32);
    pub fn uasm_il_bnez(p: *mut *mut u32, r: *mut *mut uasm_reloc, reg: u32, lid: i32);
}

pub const UASM_LABEL_INVALID: i32 = 0;

// SOURCE-COMMIT: d482bb509b7d065808de40ce78b5bca39f40b783
